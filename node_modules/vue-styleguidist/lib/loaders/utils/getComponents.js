"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = getComponents;

var _processComponent = _interopRequireDefault(require("./processComponent"));

/**
 * Process each component in a list.
 *
 * @param {Array} components File names of components.
 * @param {object} config
 * @returns {object|null}
 */
function getComponents(components, config, subComponentsPaths) {
  return components.map(function (filepath) {
    return (0, _processComponent["default"])(filepath, config, subComponentsPaths ? subComponentsPaths[filepath] : undefined);
  });
}