<script>
  const mapShipping = {}
  mapShipping.content = {{ section.settings | json }}

  window.Scoutside.shippingschedule.map = mapShipping
</script>

{% schema %}
  {
    "name": "Map Section",
    "settings": [
      {
        "label": "Desk Background",
        "type": "image_picker",
        "id": "desk_bg"
      },
      {
        "label": "Mobile Background",
        "type": "image_picker",
        "id": "mobile_bg"
      }
    ]
  }
{% endschema %}
