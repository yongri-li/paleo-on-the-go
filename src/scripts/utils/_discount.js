// const getDiscountCode = () => {
//   const currentHref = location.href
//   if (currentHref.includes('?discount=')) {
//     const discCode = currentHref.split('?discount=')[1]
//     console.log('discCode', discCode)
//     sessionStorage.setItem('discCode', discCode)
//     // const nextURL = `${currentHref}${ssDiscount}`
//     // console.log('url code here', `${currentHref}${ssDiscount}`)
//     // const nextTitle = 'Stratia Checkout'
//     // const nextState = { additionalInformation: 'Updated the URL with JS' }
//     // window.history.replaceState(nextState, nextTitle, nextURL)
//     // location.reload()
//   }
// }
// getDiscountCode()
